# Handoff prompt

Copy into Claude with this repository open:

```text
Work from the root of your demo-media-library checkout.

Read CLAUDE.md, README.md, and the linked delegation docs before making changes.

Build the initial media-library baseline as a reusable starting point for later
learning exercises and ExecDesk dogfooding. Do not implement optional exercises.

First inspect the checkout and decision register. The docs propose a catalog of
books, films, and music albums, not media uploads or playback, using Angular and
Java/Spring Boot. Confirm that scope and stack with me; do not treat proposals as
accepted decisions.

Present a compact recommendation for the remaining fields, search/edit rules,
UX, and persistence choices, with alternatives where consequential. Ask before
implementing dependent work. Do not treat silence as approval or reopen choices
already accepted.

Once those choices are resolved, proceed through implementation, documentation,
and proportionate verification without stopping for routine confirmation. Keep
code readable in focused files. Avoid unnecessary infrastructure and exhaustive
test matrices.

Stop for new consequential choices or scope changes; explain the options and
your recommendation. Do not commit, push, merge, publish, deploy, change repository
visibility, or create external issues/PRs unless I explicitly authorize it.

Finish with verified run/reset instructions, implemented capabilities, actual
checks, known limitations, and decisions made during work. Leave the result
ready for my review.
```
